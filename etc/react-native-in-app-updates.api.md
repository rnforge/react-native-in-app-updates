## API Report File for "@rnforge/react-native-in-app-updates"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function addInstallStateListener(listener: InstallStateListener): InstallStateSubscription;

// @public
export type AllowedFlows = {
    immediate: boolean;
    flexible: boolean;
};

// @public
export type AndroidDetails = {
    packageName?: string;
    playCore?: PlayCoreDetails;
};

// @public
export type AndroidUpdateOptions = {
    allowAssetPackDeletion?: boolean;
};

// @public
export type AvailabilityReason = 'update-available' | 'no-update-available' | 'developer-triggered-update-in-progress' | 'flexible-update-downloaded' | 'update-not-allowed' | 'context-unavailable' | 'activity-unavailable' | 'user-canceled' | 'unknown';

// @public
export function canCompleteFlexibleUpdate(status: UpdateStatus): boolean;

// @public
export function canOpenStorePage(status: UpdateStatus): boolean;

// @public
export function canStartFlexibleUpdate(status: UpdateStatus): boolean;

// @public
export function canStartImmediateUpdate(status: UpdateStatus): boolean;

// @public
export type Capabilities = {
    immediate: boolean;
    flexible: boolean;
    storePage: boolean;
    latestVersionLookup: boolean;
    installStateListener: boolean;
};

// @public
export function completeFlexibleUpdate(): Promise<UpdateStatus>;

// @public
export function getUpdateStatus(options?: GetUpdateStatusOptions): Promise<UpdateStatus>;

// @public
export type GetUpdateStatusOptions = {
    ios?: {
        appStoreId?: string;
        country?: string;
    };
    android?: AndroidUpdateOptions;
};

// @public
export class InAppUpdatesError extends Error {
    constructor(message: string, code: InAppUpdatesErrorCode, android?: AndroidDetails);
    readonly android?: AndroidDetails;
    readonly code: InAppUpdatesErrorCode;
}

// @public
export type InAppUpdatesErrorCode = 'invalid-input' | 'bridge-error' | 'native-error' | 'unexpected';

// @public
export type InstallStateEvent = {
    platform: Platform;
    supported: boolean;
    installStatus: InstallStatus;
    reason: InstallStateEventReason;
    bytesDownloaded?: number;
    totalBytesToDownload?: number;
    progress?: number;
    errorCode?: string;
    message?: string;
    android?: AndroidDetails;
};

// @public
export type InstallStateEventReason = UnsupportedReason | 'download-progress' | 'install-state-changed' | 'flexible-update-downloaded' | 'unknown';

// @public
export type InstallStateListener = (event: InstallStateEvent) => void;

// @public
export type InstallStateSubscription = {
    remove: () => void;
};

// @public
export type InstallStatus = 'unknown' | 'pending' | 'downloading' | 'downloaded' | 'installing' | 'installed' | 'failed' | 'canceled' | 'unsupported';

// @public
export type IosAppStoreDetails = {
    version?: string;
    trackViewUrl?: string;
    trackName?: string;
    releaseNotes?: string;
    description?: string;
    minimumOsVersion?: string;
    averageUserRating?: number;
    userRatingCount?: number;
    artworkUrl60?: string;
    artworkUrl100?: string;
    artworkUrl512?: string;
};

// @public
export type IosDetails = {
    bundleIdentifier?: string;
    appStoreId?: string;
    storeUrl?: string;
    appStore?: IosAppStoreDetails;
};

// @public
export function isUpdateAvailable(status: UpdateStatus): boolean;

// @public
export function openStorePage(options?: OpenStorePageOptions): Promise<void>;

// @public
export type OpenStorePageOptions = {
    ios?: {
        appStoreId: string;
        country?: string;
    };
};

// @public
export type Platform = 'android' | 'ios';

// @public
export type PlayCoreDetails = {
    immediateFailedPreconditions?: string[];
    flexibleFailedPreconditions?: string[];
    installErrorCode?: number;
    taskErrorCode?: number;
    updateAvailability?: string;
    installStatus?: string;
    updatePriority?: number;
    clientVersionStalenessDays?: number;
    availableVersionCode?: number;
    bytesDownloaded?: number;
    totalBytesToDownload?: number;
    immediateAllowed?: boolean;
    flexibleAllowed?: boolean;
};

// @public
export function startFlexibleUpdate(options?: StartFlexibleUpdateOptions): Promise<UpdateStatus>;

// @public
export type StartFlexibleUpdateOptions = {
    android?: AndroidUpdateOptions;
};

// @public
export function startImmediateUpdate(options?: StartImmediateUpdateOptions): Promise<UpdateStatus>;

// @public
export type StartImmediateUpdateOptions = {
    android?: AndroidUpdateOptions;
};

// @public
export function supportsInstallStateListener(status: UpdateStatus): boolean;

// @public
export type UnsupportedReason = 'unsupported-platform' | 'unsupported-os-version' | 'unsupported-install-source' | 'apk-expansion-files-unsupported' | 'missing-app-store-id' | 'play-core-unavailable' | 'store-lookup-unavailable' | 'store-lookup-timeout' | 'store-lookup-network-error' | 'store-lookup-http-error' | 'store-lookup-not-found' | 'store-lookup-invalid-response';

// @public
export type UpdateStatus = {
    platform: Platform;
    supported: boolean;
    updateAvailable: boolean | null;
    capabilities: Capabilities;
    allowed: AllowedFlows;
    reason: AvailabilityReason | UnsupportedReason;
    currentVersion?: string;
    currentBuild?: string | number;
    latestStoreVersion?: string;
    latestStoreBuild?: string | number;
    installStatus?: InstallStatus;
    android?: AndroidDetails;
    ios?: IosDetails;
};

// (No @packageDocumentation comment for this package)

```
